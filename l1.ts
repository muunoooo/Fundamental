/**
 * Example 1:

Input: num = 2736
Output: 7236
Explanation: Swap the number 2 and the number 7.
Example 2:

Input: num = 9973
Output: 9973
Explanation: No swap.
 */

function maximumSwap(num: number): number {
    let x = num.toString()
    for (let i = 0; i <=1 ; i++) {
        x.
    }
        
};

function swap(num: number): number {
    let x = Array.from(num.toString())
    let y = x.sort((a,b) => b.localeCompare(a))

    return parseInt(y.join())
};