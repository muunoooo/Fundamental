// Change this data to test with type of inputs
let data: any = false ;  

if (typeof data === "string") {
    console.log(1);
} else if (typeof data === "number") {
    console.log(2);
} else {
    console.log(3);
}
